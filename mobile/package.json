{
  "name": "trio-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor wrapper for the Trio multiplayer card game (Android + iOS).",
  "scripts": {
    "sync-web": "node scripts/sync-web.js",
    "cap": "cap",
    "build": "npm run sync-web && cap sync",
    "android": "npm run build && cap open android",
    "ios": "npm run build && cap open ios",
    "run:android": "npm run build && cap run android",
    "run:ios": "npm run build && cap run ios"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/app": "^6.0.1",
    "@capacitor/core": "^6.1.2",
    "@capacitor/ios": "^6.1.2",
    "@capacitor/splash-screen": "^6.0.2",
    "@capacitor/status-bar": "^6.0.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
